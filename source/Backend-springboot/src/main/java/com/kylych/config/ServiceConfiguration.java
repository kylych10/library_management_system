package com.kylych.config;

// This configuration class is no longer needed.
// ReservationService and NotificationService are injected lazily
// directly inside BookLoanServiceImpl using @Autowired @Lazy.
